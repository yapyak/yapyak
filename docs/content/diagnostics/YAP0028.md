---
title: YAP0028
name: Locale-set ignored
severity: warning
summary: setLocale() called with a value not in the configured locales
---


`setLocale()` was called with a value that isn't one of your configured locales. The call was ignored.

Validate locale values before passing them to `setLocale()`, or add the missing locale to your yapyak config.
