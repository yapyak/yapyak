---
title: YAP0028
name: Locale-set ignored
severity: warning
summary: setLocale() value isn't in the configured locales
---


`setLocale()` is called with a value that isn't one of your configured locales. The call is ignored.

Validate locale values before passing them to `setLocale()`, or add the missing locale to your yapyak config.
