---
title: YAP0023
name: Cookie writer missing
severity: warning
summary: setLocale() runs on server outside withResponse scope
---


`setLocale()` runs on the server outside a `withResponse` scope. The cookie isn't set.

Install the matching [SSR adapter](/guide/adapters/overview) to wire up cookie writing per request.
