---
title: YAP0025
name: Local-storage write fails
severity: warning
summary: setLocale() can't write to `localStorage`
---


`setLocale()` can't write to `localStorage`. Common causes: quota exceeded, Safari private mode, or storage disabled by the user.

The in-memory locale updates but won't survive a reload. Consider falling back to cookie persistence for affected users.
