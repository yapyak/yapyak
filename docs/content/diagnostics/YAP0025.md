---
title: YAP0025
name: Local-storage write failed
severity: warning
summary: setLocale() couldn't write to `localStorage`
---


`setLocale()` couldn't write to `localStorage`. Common causes: quota exceeded, Safari private mode, or storage disabled by the user.

The in-memory locale was updated but won't survive a reload. Consider falling back to cookie persistence for affected users.
