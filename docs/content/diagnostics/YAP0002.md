---
title: YAP0002
name: Dynamic template literal
severity: error
summary: t() source is a template literal with interpolation
---


{% diagnostics %}
t(`Hi ${name}`);  // no
t('Hi {name}', { name });  // yes
{% /diagnostics %}

The source has to be a static string literal so the compiler can extract it. Use a placeholder instead.
