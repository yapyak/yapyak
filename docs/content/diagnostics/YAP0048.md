---
title: YAP0048
name: Parse error
severity: error
summary: a framework processor reports a syntax problem in the source file
---

A framework processor's parser could not read the source file, so its `t()` calls cannot be extracted. The message and range come from the parser itself.

Fix the syntax at the reported location.
