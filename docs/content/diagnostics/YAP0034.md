---
title: YAP0034
name: Entry shape invalid
severity: warning
summary: custom translator returns the wrong shape for an entry
---


A custom translator returns something other than an object keyed by target locales for one entry. The entry is dropped and the translations are empty.

Fix the custom translator's return shape to match the expected `{ [locale]: string }` format.
