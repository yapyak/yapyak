---
title: YAP0024
name: Local-storage SSR skipped
severity: warning
summary: setLocale() runs on server with `persistence: 'local-storage'`
---


`setLocale()` runs on the server with `persistence: 'local-storage'`. Local storage is browser-only.

Use `cookie` persistence for SSR scenarios.
