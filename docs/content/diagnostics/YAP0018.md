---
title: YAP0018
name: Mixed usage
severity: error
summary: same source uses both t() and t.as() in one file
---


In the same file:

```ts
t('Open');

t.as('action', 'Open');
```

A source string can't use both `t()` and `t.as()` in one file. Pick one form per source.
