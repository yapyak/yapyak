---
title: YAP0029
name: SSR leak risk on `setLocale()`
severity: warning
summary: setLocale() runs on server in a way that may leak across requests
---


`setLocale()` runs on the server in a way that leaks across concurrent requests.

Configure `cookie` or `url` persistence so each request scopes its own locale.
