---
title: YAP0029
name: SSR leak risk on `setLocale()`
severity: warning
summary: setLocale() called on server in a way that may leak across requests
---


`setLocale()` was called on the server in a way that would leak across concurrent requests.

Configure `cookie` or `url` persistence so each request scopes its own locale.
