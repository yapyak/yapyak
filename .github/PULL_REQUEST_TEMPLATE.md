## What & why

<!-- One concern per PR. Link the issue if one exists. -->

## Checklist

- [ ] `pnpm build && pnpm check && pnpm typecheck && pnpm test` passes
- [ ] Tests added or updated for behavioral changes
- [ ] Docs updated if the public API changed
- [ ] Changeset added (`pnpm changeset`) if the change is user-facing
