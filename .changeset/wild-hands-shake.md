---
'@yapyak/vite': patch
---

Add a `root` option that points the plugin at the project directory when the bundler roots Vite somewhere else, and watch the locales directory even when it sits outside the bundler root.
